#################################################################
# several shotcuts are availables for this configuration:
#  1. move and resize window
#     up / down / left / right / return 
#     s / d / f / g / h
#  2. move window to next screen
#     [ / ]
# created by zfdang
#################################################################

# Configuration is split into the following directives:

# config (for global configurations)
config defaultToCurrentScreen true
config nudgePercentOf screenSize
config resizePercentOf screenSize

# Aliases  (to create alias variables)
alias full screenOriginX;screenOriginY screenSizeX;screenSizeY
alias lefthalf screenOriginX;screenOriginY screenSizeX/2;screenSizeY
alias lefthalfsmall screenOriginX;screenOriginY screenSizeX/3;screenSizeY
alias lefthalflarge screenOriginX;screenOriginY 2*screenSizeX/3;screenSizeY
alias righthalf screenOriginX+screenSizeX/2;screenOriginY screenSizeX/2;screenSizeY
alias righthalfsmall screenOriginX+2*screenSizeX/3;screenOriginY screenSizeX/3;screenSizeY
alias righthalflarge screenOriginX+screenSizeX/3;screenOriginY 2*screenSizeX/3;screenSizeY
alias middle screenOriginX+screenSizeX/5;screenOriginY 3*screenSizeX/5;screenSizeY
alias tophalf screenOriginX;screenOriginY screenSizeX;screenSizeY/2
alias bottomhalf screenOriginX;screenOriginY+screenSizeY/2 screenSizeX;screenSizeY/2
# the following corners are not used at the moment
alias topleft corner top-left resize:screenSizeX/2;screenSizeY/2
alias topright corner top-right resize:screenSizeX/2;screenSizeY/2
alias bottomleft corner bottom-left resize:screenSizeX/2;screenSizeY/2
alias bottomright corner bottom-right resize:screenSizeX/2;screenSizeY/2

alias hyper ctrl;shift;alt;cmd

# move and resize Bindings
bind up:${hyper} move ${tophalf}
bind down:${hyper} move ${bottomhalf}
bind left:${hyper} move ${lefthalf}
bind right:${hyper} move ${righthalf}
bind return:${hyper} move ${middle}

bind s:${hyper} move ${lefthalfsmall}
bind d:${hyper} move ${lefthalflarge}
bind f:${hyper} move ${full}
bind g:${hyper} move ${righthalflarge}
bind h:${hyper} move ${righthalfsmall}

# move among different screens
bind [:${hyper} move ${middle} 0
bind ]:${hyper} move ${middle} 1

# Nudge Bindings
bind h:${hyper} nudge -1% +0
bind l:${hyper} nudge +1% +0
bind j:${hyper} nudge +0 +1%
bind k:${hyper} nudge +0 -1%

# App Bindings
bind i:${hyper} show 'iTerm'
bind b:${hyper} show 'Chrome'
bind o:${hyper} show 'Outlook'

# Window Hints
bind /:${hyper} hint QWERTYUIOP
